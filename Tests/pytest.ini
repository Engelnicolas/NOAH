[pytest]
addopts = -q
filterwarnings =
    ignore:datetime\.datetime.utcnow\(\) is deprecated:DeprecationWarning
    ignore:PytestReturnNotNoneWarning
markers =
    integration: requires the real sops binary (excluded from standard CI)
