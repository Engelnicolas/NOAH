---
# PLACEHOLDER — encrypt with `sops --encrypt --in-place` before commit.
# Populated by `noah secrets push --service headlamp` after the
# Authentik OIDC provider for Headlamp has been provisioned.
apiVersion: v1
kind: Secret
metadata:
  name: headlamp-oidc
  namespace: headlamp
type: Opaque
stringData:
  clientID: REPLACE_WITH_OIDC_CLIENT_ID
  clientSecret: REPLACE_WITH_OIDC_CLIENT_SECRET
