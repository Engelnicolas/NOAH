---
# PLACEHOLDER — encrypt this file with `sops --encrypt --in-place`
# before committing. The two Age recipients in flux-repo/.sops.yaml
# determine who can decrypt it.
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
type: Opaque
stringData:
  api-token: REPLACE_WITH_CLOUDFLARE_TOKEN
