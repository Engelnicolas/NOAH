---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # authentik-provisioner-script ConfigMap is generated by the parent
  # gitops/apps/kustomization.yaml (configMapGenerator, hashed name) so edits
  # to the script re-trigger this Job.
  - provision-job.yaml
  - hubble-ingress.yaml
  - auth-proxy-configmap.yaml
  - auth-proxy-deployment.yaml
